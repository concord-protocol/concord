# CORD-05: Invites

How members are handed the keys that make them members, an invite is a shareable URL whose keys live in an encrypted bundle on relays, the link itself carries only a public locator and an off-network unlock token, never the direct keys, so anyone the link reaches can join and nobody who lacks it can, this also means an Invite Link can be revoked without the need to reroll access keys. And because the link is only text, it travels anywhere text does, including over Nostr itself, giftwrapped to a specific npub as a Direct Invite (§6).

----

## 1. The Bundle

An invite delivers a bundle to relays, encrypted by an off-network URL token:
```
CommunityInvite {
  community_id, owner, owner_salt,        // owner proof: community_id == sha256(owner || salt)
  community_root, root_epoch,             // the base access key + its current epoch
  channels: [{ id, key, epoch, name }],   // the granted Channels
  relays,                                 // where the Community publishes
  name, icon,                             // preview, so a parked invite can render
  expires_at,                             // optional, unix ms: past it, the preview still renders, joining refuses
  creator_npub, label                     // optional attribution, echoed in the joiner's Join (below)
}
```
The inviter's own identity is irrelevant to trust, the `community_id` self-certifies the owner (CORD-02), so a bundle can't smuggle a false owner or a fake key for a real Community, a mismatching bundle (its `owner` and `owner_salt` failing to reproduce the `community_id`) is simply refused.

A bundle is attacker-crafted input reached by following a link, so a client MUST bound it before allocating: reject a bundle carrying more than a sane channel count (Vector's ceiling is 256), and truncate `relays` to the Community's cap (CORD-02 §6). Absent bounds, a hostile link is an unbounded-allocation and connect-storm vector.

Attribution is the exception, by choice: a bundle may name its creator and a human label for the link ("Reddit", "Conf 2026"), and an accepting joiner echoes both in their Guestbook Join (CORD-02 §5), which is what makes per-link usage counters possible, all of it inside the token-encrypted bundle, visible to link-holders alone.

An invite bundle is passive, it waits on relays to be *fetched* rather than being pushed to anyone, so a user only encounters one by actively following a link, and even then the client merely pulls it to preview the Community, nothing joins, subscribes, or announces presence until the user explicitly accepts.

## 2. The Link

An invite is a URL in two parts, a public locator in the path and a secret in the `#fragment`:
```
$BASE/invite/<naddr>#<fragment>
```
The **naddr** is a bare NIP-19 addressable-event pointer, `(kind 33301, link_signer, "")`, naming exactly where the bundle lives; it's a locator, not a secret (the Registry publishes locators to every member anyway, §5), so it can ride in the path in the open. The **fragment** carries a random 16-byte **unlock token** (plus the bootstrap relays, §3), and a fragment is never sent to any server, it stays client-side, so the base domain and the relays alike see where a bundle sits but can never open one. The base is interchangeable: the same naddr and fragment open on `vectorapp.io/invite/…`, on Soapbox's Armada web base, or on any deeplink redirect domain anyone cares to run (as Matrix does with its many `matrix.to`-alikes), only the naddr and the fragment are protocol and any client recognizing an invite must respect them verbatim. The token derives exactly one thing:
```
bundle_key = hkdf(token, "concord/invite-key")   // decrypts the bundle
```
Minting a link mints a fresh **link signer**, a keypair used for nothing else, its secret kept in the creator's Invite List (§4). The bundle is posted as an addressable event authored by `link_signer` with an *empty* `d` identifier — the per-link pubkey alone makes the coordinate unique, so the naddr carries no identifier bytes and no relay entries (relays travel compactly in the fragment, §3), keeping it as short as an naddr gets. Fetchers locate the bundle by the full addressable coordinate `(kind, author, d)`, which is what makes the guard airtight in both directions: a squatter *can't* squat (a different author is a different coordinate, their spam never even matches the filter), and a link-holder can't replace or tombstone the bundle either, because posting to the coordinate requires the `link_signer` secret, held only by the creator (synced across their devices inside the self-encrypted Invite List, §4). This deletes an entire threat class an earlier draft accepted as a tradeoff (its signer derived from the token, so any invitee could kill or corrupt the link they were trusted with): now the worst a malicious invitee can do is join, and the self-certifying `community_id` (§1) already ensured that even a compromised *creator* can't smuggle a false owner. On the wire, this is one of the few Concord events relays see bare (CORD-02 Appendix B):
```jsonc
{ "kind": 33301, "pubkey": "<link_signer's pubkey>",
  "content": "<nip44_encrypt(bundle_key, the §1 bundle as JSON)>",
  "tags": [ ["d", ""], ["vsk", "6"] ] }   // vsk 9: the revocation tombstone (below)
```

Because the coordinate is stable, the link itself never changes, the creator re-posting under it *refreshes the bundle* it addresses (fresh keys behind the same URL, e.g. after a Rekey), so a link shared once survives every rotation. Retiring a link works the same way: the coordinate is re-posted as a **revocation tombstone** (a sub-kind flag, still signer-signed, so the creator only), replacing the bundle so a fetcher finds the grave instead of keys, and unlike a relay deletion (best-effort, quietly ignorable) the tombstone is exactly as durable as the bundle it replaced. Retiring the last live link is what flips a Community back to Private (a Refounding, CORD-06).

Clients may quickly identify whether an Invite Link is still live or not by checking for a tombstone at the addressable bundle.

## 3. The Relay Dictionary

An invite must include the Community's relays for immediate reachability, but inserting each one in the Invite URL inflates it massively, often past what a length-restricted platform will accept and into an eyesore besides (NIP-19 could carry them as naddr relay entries, but only as full plaintext URLs, the same bloat in a different pocket), so Concord defines a small, trusted **relay dictionary** every client knows, and a community using these relays (a default community) references them by a *single byte* rather than its full URL.

The stock set (four primaries, two Vector and two Soapbox) is selected by one flag, so the common invite carries zero additional relay bytes:
```
1 = wss://jskitty.com/nostr          (Vector)
2 = wss://asia.vectorapp.io/nostr    (Vector)
3 = wss://relay.ditto.pub            (Soapbox)
4 = wss://relay.dreamith.to          (Soapbox)
```
The fragment encodes `[version][flags][relays?][token:16]` as base64url with no padding. `version` is a format byte that also selects the dictionary generation, it's `4` (a client MAY reject any lower value as a legacy link rather than decode it against the wrong dictionary). The `flags` bit selects the stock set, in which case zero relay bytes follow. Otherwise a `count` byte precedes `count` entries, each a leading byte:
```
1..=254   a dictionary id (the relay is one byte, no literal)
0         a wss-implied literal: [len][host bytes], "wss://" re-prepended on decode
255       a verbatim literal:    [len][full URL bytes], for ws:// or exotic schemes
```
The fragment carries at most **3 bootstrap relays**, it only needs to *find* the bundle, which then carries the Community's authoritative relay set (CORD-02 §6).

None of this is forced, the dictionary is a *default*, not a requirement, a client or user can skip the primaries entirely and encode their own relays inline (the escape bytes above), the stock set simply lets a non-technical user create and join a Community with zero configuration, while anyone wanting full control keeps it.

The dictionary is versioned, so it can grow without breaking older links, and both Vector and Soapbox ship it identically, an invite minted by either client opens in the other.

## 4. The Invite List

A creator's minted links sync across their devices (and their *clients*, two apps can serve one npub) as the **Invite List**, the kind `13303` replaceable (CORD-02, Appendix B), NIP-44-encrypted to self, its content the creator's private bookkeeping:
```jsonc
{ "entries": [
    { "token": "<hex>",              // the link's unlock secret AND its merge key
      "signer_sk": "<hex>",          // the link_signer secret: refreshing or retiring the bundle needs it (§2)
      "community_id": "<hex>",
      "url": "<the shareable link>",
      "label": "Reddit",             // optional, the link's human name
      "created_at": 1719800000,
      "expires_at": 1722400000 } ],  // optional
  "tombstones": [ { "token": "<hex>", "community_id": "<hex>" } ] }
```
Two copies merge without coordination: the token is the merge key, an entry is immutable once minted, tombstones union, and a tombstone always beats an entry, terminally, so a stale device can never resurrect a revoked link. And because two clients can share this one document, the round-trip discipline applies (CORD-02 §6): preserve what you don't understand.

## 5. The Registry

The Invite List above is the creator's *private* bookkeeping, the **Registry** is its member-facing shadow: every creator of live public links also publishes a Control Plane entity (`vsk 8`, CORD-04) listing them *into the Community*, at a coordinate bound to the creator (CORD-02, A.6), so each creator owns exactly their own list and nobody can forge entries into anyone else's. Its content is the live links' *coordinates* only:
```jsonc
["<link_signer pubkey hex>", "<link_signer pubkey hex>"]   // coordinate authors (kind 33301, d empty, §2), never tokens, URLs, or signing secrets
```
Members fold every creator's Registry (honored only while its author holds `CREATE_INVITE`) into one aggregate active-set, and that set is the **Public/Private source of truth**: non-empty means a live link exists somewhere and the Community is Public, empty means Private, and retiring the last live link (§2) is precisely what empties the set and triggers the Refounding (CORD-06). A Registry edit accompanies every mint and every retire, and because it carries locators rather than secrets, members can *see* that links exist without gaining the ability to use one.

## 6. Direct Invites

Everything above assumes the link travels out-of-band, pasted into whatever channel already connects inviter and invitee, but Nostr itself is such a channel: a **Direct Invite** delivers the link to a specific npub as a *standard* NIP-59 giftwrap — ephemeral wrap author, the recipient in the `p` tag, a kind `13` seal, the classic shape, **not** the reversed stream wrap of CORD-01 — so an invite can reach someone the inviter knows only by their Nostr identity. Only the transport changes: the rumor carries the §2 link verbatim, and the recipient follows it exactly as a pasted one (fetch the coordinate, check for the tombstone, decrypt with the token, verify the self-certifying `community_id`), so a Direct Invite revokes with its link, refreshes with its bundle, and counts in the Registry like the link it carries. It grants nothing by itself and needs no permission of its own, sending one *is* sharing the URL, which any link-holder could always do.

The rumor is kind `3313` (CORD-02, Appendix B), unsigned inside the inviter-signed seal, which is what proves to the recipient who invited them:
```jsonc
{ "kind": 3313, "pubkey": "<inviter's real pubkey>",
  "content": "Come hang out with us!",                       // optional personal note, may be empty
  "tags": [ ["u", "$BASE/invite/<naddr>#<fragment>"] ] }     // the §2 link, verbatim
```
And the wrap carries what no Concord stream event may, an identifying outer tag, a `k` naming the rumor's kind:
```jsonc
{ "kind": 1059, "pubkey": "<ephemeral, single-use>",
  "content": "<nip44_encrypt(ephemeral↔recipient, the kind 13 seal)>",
  "tags": [ ["p", "<recipient>"], ["k", "3313"] ] }
```
The `k` tag is what makes invites **indexed**: a recipient looks up exactly their invites, `{"kinds":[1059], "#p":["<me>"], "#k":["3313"]}`, instead of decrypting everything ever p-tagged at them (NIP-17's cost), so an invite surfaces even in a client that syncs no other giftwrap traffic. The unmasking is deliberate and narrow, an observer learns that *someone* invited this npub to *some* community at roughly some time (NIP-59's timestamp tweak applies to the wrap and seal), never which community, who sent it, or whether it was accepted — and it never weakens the stream camouflage, because a Direct Invite is person-addressed NIP-59, not stream traffic, no stream wrap carries the tag (CORD-02, Appendix B). The tag is also unsigned relay-visible bytes, a hint and never authority: an invite is whatever *unwraps* to a kind `3313` rumor, a wrap whose tag lies simply wastes its own indexability, and a client decrypting its general giftwrap inbox honors an untagged invite all the same.

A pushed invite is attacker-reachable in a way a followed link never was, unsolicited by construction, so §1's passivity hardens into a rule: a client MUST render a Direct Invite from the rumor alone (the seal-verified inviter and their note) and perform *no* network action — not fetching the bundle, not connecting to a fragment relay, not resolving anything — until the user acts on it, or an unsolicited event becomes a beacon driving the client to attacker-chosen relays. The preview (name, icon) comes from the bundle *after* the user engages, exactly like a pasted link, so a spam invite gets no free rendering of its claims, and everything a fetched bundle must survive (§1's bounds, the owner proof) applies unchanged.

Delivery targets the recipient's giftwrap inbox, the relays in their kind `10050` DM-relay list (NIP-17) when one exists, their NIP-65 read relays otherwise, and the wrap MAY carry a NIP-40 expiration matching the link's `expires_at`, letting relays prune an invite that would refuse to join anyway.
