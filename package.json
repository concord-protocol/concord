{
  "name": "concord-spec-tests",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript reference model + tests asserting the claims of the CORD specs",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "nostr-tools": "^2.10.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
